'use client'
import {UploadForm} from "@/components/templates";

export default function Upload() {

    return (
        <main className="min-h-screen">
            <UploadForm/>
        </main>
    )
}
